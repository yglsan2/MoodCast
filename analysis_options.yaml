include: package:flutter_lints/flutter.yaml

# Ne pas activer prefer_const_constructors globalement : des centaines d’infos
# sans impact sur l’exécution, et `flutter analyze` échoue alors inutilement.
linter:
  rules:
    prefer_const_constructors: false
    prefer_const_literals_to_create_immutables: false
